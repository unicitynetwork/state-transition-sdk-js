/**
 * Marker interface for objects passed to verification rules. Rule
 * implementations narrow the type to their own required fields.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface IVerificationContext {}
